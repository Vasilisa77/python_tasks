# Python Basics: Практические задания

Репозиторий содержит решения базовых задач на языке Python. Проект выполнен в рамках обучения и включает работу с типами данных, модулями `math` и `random`, а также проверку кода с помощью автоматических тестов.

## 📝 Список решенных задач:

1.  **test_circle** — расчет площади круга и длины окружности по радиусу.
2.  **test_random_list** — генерация списка из 10 случайных чисел и их сортировка.
3.  **test_unique_elements** — удаление дубликатов из списка с использованием множеств (`set`).
4.  **test_dicts** — создание словаря из двух списков с помощью функции `zip`.
5.  **test_greeting** — форматирование строк с использованием f-строк.
6.  **test_rectangle** — расчет периметра и площади прямоугольника.

## Как запустить тесты

Для проверки работоспособности кода используется фреймворк `pytest`.

1. Установите pytest (если не установлен):
```bash
pip install pytest